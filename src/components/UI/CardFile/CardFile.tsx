import React, { FC } from 'react'
import cl from './CardFile.module.scss'

interface CardFileProps {
    file: File;
}

const CardFile: FC<CardFileProps> = ({file}) => {
  return (
    <div>

    </div>
  )
}

export default CardFile